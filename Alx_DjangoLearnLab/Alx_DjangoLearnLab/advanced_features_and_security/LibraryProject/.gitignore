# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
.Python/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environment
# This is crucial. Your `venv/` directory contains all your project's
# dependencies and is specific to your local setup. It can be recreated.
venv/

# MyPy
.mypy_cache/
.dmypy.json
dmypy.json

# Pytest and coverage
.pytest_cache/
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/

# Jupyter Notebook (if you use them)
.ipynb_checkpoints/

# Django specific
# Local mysql database file. This often contains development data that
# shouldn't be committed, and it can be recreated via migrations.
db.mysql

# Media files uploaded by users. These should be served from a separate
# storage solution in production and don't belong in Git.
media/

# Collected static files. These are generated by `python manage.py collectstatic`
# and are deployed separately in production.
static/

# Log files generated by your application.
*.log

# Translation files (if you use Django's internationalization)
# You typically track .po files, but .pot (template) and compiled .mo files are ignored.
*.pot
*.mo # Some people track .mo if they're pre-compiled, but generally ignored.

# Local settings file
# If you have a separate file for sensitive local settings (e.g., API keys, secret key)
# that isn't committed to the main settings.py.
# For example, if you have myproject/myproject/local_settings.py
local_settings.py

# IDE specific files
# PyCharm
.idea/
*.iml
*.iws
out/

# VS Code
.vscode/
# Note: Sometimes you might want to commit specific VS Code config files
# like `settings.json` or `launch.json` if they benefit the whole team.
# If so, you'd add exceptions like:
# !.vscode/settings.json
# !.vscode/launch.json

# Sublime Text
*.sublime-project
*.sublime-workspace

# Operating System specific files
.DS_Store # macOS
Thumbs.db # Windows

# Temporary files from various editors/tools
*~
*.bak
*.swp

#ignore the file which helps git ignore my database config
.env